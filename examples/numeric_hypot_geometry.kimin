struct Vec2 {
  x: Number
  y: Number
}

impl Vec2 {
  fn magnitude(self) -> Number {
    return hypot(self.x, self.y)
  }

  fn angle(self) -> Number {
    return atan2(self.y, self.x)
  }
}

let v = Vec2 { x: 3, y: 4 }

print(v.magnitude())
print(round(v.angle()))
