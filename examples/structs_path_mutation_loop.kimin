struct User {
  name: Text
  score: Number
}

let mut users: Array<User> = [
  User { name: "alice", score: 10 },
  User { name: "bob", score: 20 }
]

for i, user in users {
  users[i].score += 1
}

print(users[0].score)
print(users[1].score)
