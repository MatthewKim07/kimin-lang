let mut values = [0, 0, 0]
let mut i: Number = 0

let duration: seconds = 3
let dt: seconds = 1

simulate duration step dt {
    values[i] += i + 10
    i += 1
}

print(values[0])
print(values[1])
print(values[2])
